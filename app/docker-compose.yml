services:
  app:
    image: nginx:stable-alpine
    container_name: spec-aligned-ai
    ports:
      - "80:80"
    volumes:
      - ./backend:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
      - horizon
    networks:
      - said-network

  php:
    image: said-php
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: spec-aligned-ai-php
    volumes:
      - ./backend:/var/www/html
      - ./.env:/var/docker/.env
      - /var/run/docker.sock:/var/run/docker.sock
      - ${SAID_ROOT:?SAID_ROOT_not_set}:/said-projects
    environment:
      - DB_CONNECTION=pgsql
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_DATABASE=said
      - DB_USERNAME=said
      - DB_PASSWORD=said_secret
      - S3_ENDPOINT=http://minio:9000
      - S3_KEY=said_admin
      - S3_SECRET=said_secret
      - S3_BUCKET=said-specs
      - S3_REGION=us-east-1
    depends_on:
      - postgres
      - minio
    networks:
      - said-network

  horizon:
    image: said-horizon
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: spec-aligned-ai-horizon
    volumes:
      - ./backend:/var/www/html
    environment:
      - DB_CONNECTION=pgsql
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_DATABASE=said
      - DB_USERNAME=said
      - DB_PASSWORD=said_secret
      - S3_ENDPOINT=http://minio:9000
      - S3_KEY=said_admin
      - S3_SECRET=said_secret
      - S3_BUCKET=said-specs
      - S3_REGION=us-east-1
    command: php /var/www/html/artisan horizon
    depends_on:
      - php
      - postgres
      - redis
      - minio
    networks:
      - said-network

  redis:
    image: redis:7-alpine
    container_name: spec-aligned-ai-redis
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data
    networks:
      - said-network

  postgres:
    image: postgres:16-alpine
    container_name: spec-aligned-ai-postgres
    environment:
      - POSTGRES_DB=said
      - POSTGRES_USER=said
      - POSTGRES_PASSWORD=said_secret
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - said-network

  minio:
    image: minio/minio:latest
    container_name: spec-aligned-ai-minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=said_admin
      - MINIO_ROOT_PASSWORD=said_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./data/minio:/data
    networks:
      - said-network

networks:
  said-network:
    driver: bridge
